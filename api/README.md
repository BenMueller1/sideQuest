## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the server

   ```bash
    npm run serve
   ```

## To migrate schema changes to the database

    ```bash
    npx prisma migrate dev --name init
    ```
